# 2026-05-11 작업 일지

## 수행 내용

- `docs/schema.md` 업데이트
  - 구조물·기계·부속품에 `code_key` 필드 추가
  - 외부 기계 `location`: `internal` / `external` 타입 분리, `external`은 `process_id`만 유지
  - 부속품 필드명: `type` → `code_key` 변경
- `schema/sample_real_01.json` 신규 작성 — `docs/wip_sample_realdata.md` 전체 반영
  - 공정 2개 (A, B), 구조물 3개, 기계 25개, 파이프 31개
  - A공정 슬러지 순환 4구간 TEE 패턴, 공기 2계통, 원수 1:1 직결 배관 모델링
  - B공정 PMP0602 흡입 매니폴드(원수), PMP0601 흡입(슬러지) 모델링
  - 부속품 규칙(운전/예비 구분) 전체 반영
- 모든 파이프 관경 초기값 `100A` 설정

## 규칙 변경 / 확인 사항

| 섹션 | 변경 내용 | 비고 |
|------|-----------|------|
| schema — machines.location | external 타입에서 `structure_id` 제거, `process_id`만 유지 | 흐름 기반 배치 원칙 |
| schema — accessories | `type` 필드 → `code_key` 필드로 변경 | code_key 코드표 동일 적용 |
| pipe size | 전체 파이프 `null` → `"100A"` (초기 기본값) | 실제 관경 확인 후 수정 예정 |

## 이슈 및 특이사항

- BRX01(처리기계) 연결 배관의 부속품 규칙이 별도로 정의되지 않아, 흡입/토출 측 accessories를 빈 배열로 처리
- A공정 원수(FDC01 → VAV0101) 직결 배관은 부속품 규칙 미정, 추후 확인 필요

---

## 오후 작업 — CAD 블록 연동 설계 논의 및 schema.md 추가 반영

### 수행 내용

- CAD 블록과 JSON 포트 연동 방식 설계 확정
  - 노즐 표현: 블록 내 **invisible attribute** 배치, TAG = JSON `port.id`
  - 좌표 기준: attribute 삽입점(insertion point) = 노즐 좌표
  - P&ID는 위상(topology) 도면이므로 JSON에 좌표 없음, LISP 엔진이 블록 배치 후 attribute 좌표 추출
- 내부 기계 슬롯 표현 방식 설계 확정
  - 기계 카탈로그 DB (`카탈로그_기계_v1.5.0.xlsx`) 계층 구조 확인
  - 슬롯 TAG = 내부 기계 `code_key`의 **DB `parent_key`** 값
  - 예: `M_FDC01` → parent `M_FDCT` → 구조물 블록 슬롯 TAG = `"M_FDCT"`
- `docs/schema.md` 추가 반영
  - CAD 블록 설계 규칙 섹션에 노즐 attribute 규칙 추가
  - 내부 기계 슬롯 attribute 규칙 및 예시 추가

### 규칙 변경 / 확인 사항

| 섹션 | 변경 내용 | 비고 |
|------|-----------|------|
| CAD 블록 — 노즐 | invisible attribute, TAG = port.id, 삽입점 = 노즐 좌표 | Middle Center 정렬 불필요, 삽입점 위치로 강제 |
| CAD 블록 — 내부기계 슬롯 | 슬롯 TAG = DB parent_key | code_key 자체가 아닌 부모 코드 사용 |

### 이슈 및 특이사항

- DB에서 `M_BRX01`, `M_PKA0102` 일부 항목의 `parent_key`가 `=D222` 등 깨진 값으로 확인됨 — 추후 DB 데이터 정합성 확인 필요
- `code_hierarchy.json` 생성(Excel → JSON 변환) 작업은 추후 진행 예정

## 다음 작업 예정

- 실제 관경 확인 후 파이프별 `size` 수정
- BRX01 및 VAV0101 연결 배관 부속품 규칙 확인 및 반영
- 계측기(instruments) 데이터 수집 및 JSON 반영
- `rules/code_hierarchy.json` 생성 (카탈로그 Excel 기반)
- DB `parent_key` 데이터 오류 항목 확인 및 수정
